# 動作手順書

本書は、商品管理システムの起動方法および動作確認手順について記載したものです。

---

## 1. 動作環境

| 項目 | 内容 |
|---|---|
| OS | macOS |
| Java | Java 17 |
| Webサーバ | Apache Tomcat 10 |
| DB | MySQL 8.0 |
| DB操作ツール | MySQL Workbench |
| ブラウザ | Google Chrome |

---

## 2. システム起動手順

### 2.1 データベースの起動
1. MySQL Server を起動する  
2. MySQL Workbench を起動し、`Local instance 3306` に接続する  

---

### 2.2 データベース作成・初期データ投入

#### ① スキーマ作成（初回のみ）
```sql
CREATE DATABASE product_management;
```

#### ②　テーブル作成
```sql
CREATE TABLE categories (
  id INT AUTO_INCREMENT PRIMARY KEY,
  category_name VARCHAR(50) NOT NULL
);

CREATE TABLE products (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  price INT NOT NULL,
  stock INT NOT NULL,
  category_id INT,
  FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

※すでに作成済みの場合は本手順を省略する。


#### ③サンプルデータ投入
```sql
INSERT INTO categories (category_name) VALUES
('家具'), ('食品'), ('書籍'), ('衣類'), ('家電');

INSERT INTO products (name, price, stock, category_id) VALUES
('ソファ', 30000, 5, 1),
('米', 2000, 50, 2),
('小説', 1500, 20, 3),
('Tシャツ', 3000, 50, 4),
('洗濯機', 60000, 10, 5);
```

--- 

## 3. アプリケーション起動手順
1.Eclipse を起動する
2.対象プロジェクトを開く
3.Tomcat を起動する
4.ブラウザで以下URLにアクセスする

`http://localhost:8080/development_project/login`

---

## 4. 動作確認手順

### 4.1 ログイン機能
- ユーザーID：test
- パスワード：test

ログイン成功後、商品一覧画面（/products）に遷移することを確認する。

### 4.2 商品一覧表示
- /products にアクセス
- 商品一覧が表示されることを確認する

### 4.3 商品登録
1. 「商品登録」リンクをクリック
2. 商品情報を入力し登録
3. 商品一覧に反映されることを確認する

### 4.4 商品編集
1. 商品一覧の「編集」をクリック
2. 編集画面に既存の商品情報が表示されることを確認
3. 内容を変更して保存
4. 商品一覧に変更内容が反映されることを確認する

### 4.5 商品削除
1. 商品一覧の「削除」をクリック
2. 確認画面で「はい」を選択
3. 商品一覧から削除されていることを確認する

### 4.6 ログアウト
1. 「ログアウト」リンクをクリック
2. ログイン画面に遷移することを確認
3. ログアウト後、/products に直接アクセスするとログイン画面へ遷移することを確認する

---

## 5. テスト実行手順（JUnit）
1. Eclipse の src/test/java を開く
2. 以下テストクラスを右クリックし、Run As → JUnit Test を実行する
    - LoginServletTest
    - ProductCreateServletTest
3. すべてのテストが成功（緑表示）することを確認する

---

## 6. 補足事項
- カテゴリ別フィルタリング機能は未実装のため、該当テストは未実施とする
- テスト結果は「テスト結果報告書」およびスクリーンショットにて別途記載する